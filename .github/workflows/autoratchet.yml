name: autoratchet

on:
  schedule:
    - cron: "5 7 * * *" # daily at 12:05am PT (7:05am UTC), after habits sync
  workflow_dispatch: # manual trigger

jobs:
  ratchet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - run: uv run autoratchet.py
        env:
          BEEMINDER_AUTH_TOKEN: ${{ secrets.BEEMINDER_AUTH_TOKEN }}
          BEEMINDER_USERNAME: ${{ secrets.BEEMINDER_USERNAME }}
          MAX_BUFFER_DAYS: "1"
          BEEMINDER_GOALS: "duolingo,headspace,lifts,running2,habits"
